package command

/*
1. Listen for messages
2. When receiving a message do something after an optional timeout
   a) Send some message to a channel
   b) Send some PM to a person
   c) Delete my own response
   d) Delete the commanding message
   e) Grant some role to some player
   f) Remove some role from some player.
*/
type Command struct {
    
}

func (c *Command) DoSomething() {
    
}